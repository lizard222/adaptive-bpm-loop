# REFLEX-BPM

**Адаптивное управление бизнес-процессами вуза: система исполняет регламентированные процессы, ведёт журнал событий, сама находит в нём расхождения с регламентом и корректирует параметры процессов на следующий цикл.**

> Прототип к магистерской диссертации «Адаптивное управление бизнес-процессами вуза на основе интеллектуального анализа журналов событий и многоагентного управления исполнением процессов» (ВолгГТУ, кафедра САПРиПК).

---

## Идея

Нормативный регламент («как должно быть») и реальное исполнение процесса («как получилось») всегда расходятся: этапы просрочиваются, шаги пропускаются, узкие места повторяются из семестра в семестр. Обычно это замечают вручную и постфактум.

REFLEX-BPM замыкает контур — как рефлекторная дуга:

```
исполнение BPMN-процессов ──► журнал событий (XES)
        ▲                            │
        │                            ▼
корректировки параметров ◄── интеллектуальный анализ (pm4py):
(сдвиг сроков, новые         discovery + conformance checking —
 контрольные точки)          где и насколько факт разошёлся с регламентом
```

Найденные систематические отклонения автоматически превращаются в предложения по корректировке (режим «система советует — человек решает»), и следующий цикл процесс идёт уже с уточнёнными параметрами. Главный научный результат работы — алгоритм преобразования расхождений в корректировки и экспериментальное подтверждение того, что метрики (доля просрочек, среднее опоздание, число эскалаций) улучшаются от цикла к циклу.

## Пилотные процессы

Система намеренно ограничена **двумя** регламентированными процессами кафедры (по реальным нормативным документам):

| Процесс | Почему выбран |
|---|---|
| **Организация защиты ВКР** | жёсткие нормативные дедлайны («приказ о допуске — за 14 дней до ГЭК»), много участников, эскалации |
| **Планирование учебной нагрузки** | цикличность 2 раза в год — проверка переносимости контура на другой тип процесса |

## Возможности

- 🗂 **Каталог процессов** — исполняемые модели BPMN 2.0 с метаданными: роли, сроки, шаблоны документов, правила запуска
- 🚀 **Автозапуск** — Планировщик стартует экземпляры по академическому календарю (его параметры — объект адаптации)
- ⚙️ **Исполнение** — на каждый экземпляр порождается агент процесса, ведущий BPMN как конечный автомат
- ✅ **Задачи для людей** — личный кабинет: список задач, сроки, структурированные действия («Утвердить» / «Отклонить» с причиной)
- ⏰ **Контроль сроков** — Контролер: напоминания и лестница эскалации (исполнитель → руководитель → уровень выше)
- 📄 **Документы из шаблонов** — проекты приказов и служебных записок (.docx) с автоподстановкой данных экземпляра
- 📊 **Журнал событий** — каждое действие: `case_id, activity, timestamp, resource`; экспорт в XES
- 🔁 **Контур адаптации** — анализ журнала за цикл → классификация расхождений → корректировки параметров (`propose`)
- 🧪 **Имитационный режим** — прогон N «семестров» на синтетических исполнителях с фиксируемым seed — среда воспроизводимого эксперимента

## Архитектура

```
Веб-интерфейс (React: кабинет задач, панель расхождений и корректировок)
        │
API-шлюз (FastAPI: REST + WebSocket; JWT-авторизация по ролям)
        │
Оркестратор процессов (SpiffWorkflow: исполнение BPMN 2.0;
                       состояние экземпляров — в PostgreSQL)
        │
┌───────────────────────────────┐    ┌────────────────────────────────┐
│ Слой агентов (SPADE, FIPA ACL)│◄──►│ Сервис анализа (pm4py)         │
│ • Планировщик    • Контролер  │    │ • discovery по журналу         │
│ • Агент процесса (×N)         │    │ • conformance vs нормативная   │
│ • Шаблонизатор   • Интерфейсы │    │ • расхождения → корректировки  │
└───────────────────────────────┘    └────────────────────────────────┘
        │
Слой данных (PostgreSQL: модели, состояние, event_log, версии параметров;
             Redis Streams — шина; файловое хранилище документов)
```

Принципы: **детерминированное ядро** (никаких LLM в оркестровке и документах), **прослеживаемость** (каждое действие агента → сообщение → правило), **стандарты** (BPMN 2.0, FIPA ACL, XES), **воспроизводимость** (Docker Compose, фиксируемый seed), **только обезличенные/синтетические данные**.

## Стек

| Компонент | Технология |
|---|---|
| BPMN-движок | [SpiffWorkflow](https://github.com/sartography/SpiffWorkflow) |
| Агенты (FIPA ACL) | [SPADE](https://github.com/javipalanca/spade) |
| Анализ журналов | [pm4py](https://github.com/pm4py/pm4py) |
| API | FastAPI (REST + WebSocket, JWT) |
| БД / шина | PostgreSQL / Redis Streams |
| Документы | docxtpl |
| Frontend | React |
| Имитация | Python + numpy (генератор журналов, seed) |
| Инфраструктура | Docker Compose |

## Структура репозитория

```
api/          FastAPI: конфигурация, JWT-авторизация, ролевой доступ (T-30);
              tasks.py + corrections.py — REST-слой кабинета задач (E2a/T-37)
orchestrator/ оркестратор BPMN: запуск/тик/выполнение задач, персистентность (T-31)
eventlog/     экспорт event_log в XES для pm4py (T-32)
agents/       SPADE-агенты: Планировщик, Контролер, Аналитик-Адаптер, Шаблонизатор,
              Интерфейсный (T-33, T-34, T-42, T-36, E2a), params_store.py (E3),
              dev-обвязка для XMPP
mining/       конвейер анализа (T-40) + алгоритм «расхождения → корректировки» (T-41, главный научный результат)
simgen/       генератор синтетических журналов: профиль исполнителя, цикл симуляции (T-35)
infra/prosody/ конфигурация локального XMPP-сервера для агентов (dev-only, без TLS)
bpmn/         BPMN-модели: demo/ — для смок-тестов оркестратора; здесь появятся
              исполняемые модели двух процессов кафедры + трассировка (T-22, T-23)
db/init/      SQL миграции: event_log (append-only), process_instances,
              process_params_current (E3), pending_decisions (E2a)
spikes/       спайки-исследования с зафиксированными находками (FINDINGS.md)
experiment/   сценарии эксперимента, метрики, результаты прогонов, графики (plots.py, RESULTS.md)
templates/    шаблоны документов кафедры (.docx) + рендер (T-36); сейчас — синтетический плейсхолдер
docs/         документация проекта
```

## Быстрый старт (планируемый)

```bash
git clone https://github.com/<user>/adaptive-bpm-loop && cd adaptive-bpm-loop
docker compose up            # PostgreSQL + Redis + API + агенты + фронтенд
python -m simgen --cycles 8 --seed 42   # имитация 8 семестров
python -m mining.report                 # отчёт: метрики по циклам, до/после адаптации
```

## Порядок разработки и статус

Порядок отражает зависимости (ID задач — из `project_plan.md` диссертации). Отметка ставится, когда артефакт работает и проверен.

**Фаза A — фундамент**
- [x] A1 (T-30) Каркас: Docker Compose (PostgreSQL + Redis), скелет FastAPI, JWT-роли — *смоук-тест 9/9 (JWT, ролевая защита); контейнеры healthy, `/health` — всё ok, append-only триггер `event_log` проверен*
- [x] A2 (T-28) ⚠️ Спайк SpiffWorkflow: граничные таймеры и эскалации — **движок тянет, запасной план не нужен**; прогон 13/13, детали и находки: `spikes/spiff_timer_spike/FINDINGS.md`

**Фаза B — исполнение процессов**
- [x] B1 (T-31) Оркестратор: SpiffWorkflow исполняет BPMN, состояние экземпляров в PostgreSQL — *`orchestrator/` (start_instance / tick / complete_task / get_state), таблица `process_instances`, сериализация переживает рестарт (спайк 10/10 — `spikes/serialization_spike/`), сквозной смоук 9/9 (`python -m orchestrator.smoke_test`); попутно найдено и исправлено: журнал фильтрует служебные псевдо-задачи движка (BoundaryEventSplit/Join, EndJoin) — логируются только настоящие элементы BPMN*
- [x] B2 (T-32) Журналирование: `event_log` на каждое событие (реализовано внутри оркестратора B1) + экспорт XES — `eventlog/xes_export.py`, round-trip через pm4py проверен (`python -m eventlog.smoke_test`, 5/5). Находка: jsonb-словарь `attributes` ломает конвертер pm4py (KeyError: 'value') — сериализуется в JSON-строку перед экспортом
- [x] B3 (T-33) Агент Планировщик: запуск экземпляров по расписанию (LaunchRule — минимальный эквивалент `process_params`, полная версия в T-25) + продвижение движка (тик таймеров) — `agents/scheduler_agent.py`
- [x] B4 (T-34) Агент Контролер: отслеживает срабатывание контрольных точек (напоминание/эскалация) через `event_log`, рассылает FIPA-уведомления — `agents/controller_agent.py`. Сквозной смоук Планировщик+Контролер 6/6, устойчиво на 3 прогонах подряд (`python -m agents.smoke_test`): запуск → напоминание → эскалация → уведомления получены адресатом
  - Инфраструктура: Prosody (XMPP) добавлен в `docker-compose.yml`; связность SPADE-агентов проверена отдельным спайком 6/6 — три недокументированных фикса (баг образа с правами на TLS-сертификат, серверная и клиентская политики против PLAIN-логина без шифрования) в `spikes/spade_xmpp_spike/FINDINGS.md`, обход — `agents/xmpp_dev.py`
- [x] B5 (T-35) Генератор синтетических журналов v1 — `simgen/` (профиль исполнителя: логнормальная задержка + вероятность пропуска; цикл симуляции на управляемом времени; CLI `python -m simgen.run`). Воспроизводимость по seed подтверждена смоук-тестом 4/4 (`python -m simgen.smoke_test`, стабильно на 2 прогонах). По пути: спайк управляемого времени (freezegun) 3/3 — таймеры в днях/неделях резолвятся мгновенно, без реального ожидания (`spikes/sim_clock_spike/`); попутно найден и исправлен более серьёзный баг — `event_log.ts` был `DEFAULT now()` на стороне PostgreSQL (не ловится freezegun), под управляемым временем это слепило бы все события в одну метку; теперь `ts` пишется явно из Python
- [x] B6 (T-38) Подключение второго процесса («Планирование нагрузки», по духу — реальная модель кафедры в T-23) конфигурацией — `bpmn/demo/workload_planning_days.bpmn` (ДВЕ последовательные контрольные точки, calculate_load → distribute_load) + `mining/control_points.WORKLOAD_CONTROL_POINTS`. Ни один файл orchestrator/agents/mining не менялся — только новый BPMN и новая запись конфигурации (NFR-9 подтверждён). Заодно закрыт реальный пробел в покрытии C3: там ни разу не проверялось несколько ОДНОВРЕМЕННЫХ `propose` в одном цикле (в demo_process_days всего одна контрольная точка). Смоук-тест 7/7, стабильно (`python -m agents.second_process_smoke_test`): конвейер считает обе точки раздельно, обе получают предложения одновременно (два parallel thread), стаб «завкафедрой» принимает по `calculate_load` и отклоняет по `distribute_load` — ответы на разные thread не путаются, применяется только принятое. По пути найдено и исправлено: `escalate_calc` — прерывающее событие, экземпляр, эскалированный на первом шаге, до второго не доходит («воронка») — при малой выборке (20 экз.) случайно не набиралось порога для второй точки; не баг, а требование к размеру выборки, задокументировано в тесте. Также найден и исправлен настоящий баг: `simgen/run.py._collect_report` считал напоминания/эскалации по жёстко зашитым именам `'remind'`/`'escalate'` — для процесса с другими именами активностей отчёт молча показывал нули; исправлено параметрами `reminder_activities`/`escalation_activities` (с обратной совместимостью по умолчанию)
**Фаза C — научное ядро**
- [x] C1 (T-40) Конвейер анализа pm4py: conformance (alignments) + временной срез по напоминаниям/эскалациям — `mining/conveyor.py`. Смоук-тест на данных генератора 6/6, стабильно (`python -m mining.smoke_test`). Две находки по пути (обе задокументированы в коде): (1) pm4py помечает переходы сети Петри человекочитаемым BPMN `name`, а не `id` — у нас `event_log.concept:name` = id (SpiffWorkflow), несовпадение меток обнуляло fitness/precision вообще; исправлено нормализацией меток после конвертации; (2) в conformance нельзя скармливать административные события журнала (`process_instance`, `start`, `end_main` — их нет в модели) — системно портили precision; отфильтрованы по множеству меток, реально существующих в модели. Известный оставшийся предел (не баг, задокументирован): pm4py не вполне точно конвертирует граничные BPMN-события в сеть Петри — экземпляры с эскалацией получают fitness=0.667 детерминированно вместо 1.0; доменный сигнal просрочки (напоминание/эскалация) точен и не зависит от этого ограничения — он читается прямо из журнала, не через структурный conformance
- [ ] C1b (T-40, опционально) Discovery (inductive miner): восстановление фактической модели из журнала — не реализовано в v1, приоритет ниже conformance
- [x] C1c (доработка C1) Статистика по контрольным точкам обобщена с уровня «весь экземпляр» до уровня «конкретный шаг» (`mining/control_points.py`, `ConveyorReport.control_points`) — нужно для C2, чтобы отличать просрочку конкретного этапа, а не всего процесса
- [x] C2 (T-41) ⭐ Алгоритм «расхождения → корректировки»: классификация (систематическая просрочка / пропуск шага), конфигурируемые пороги, обоснование каждого предложения — `mining/corrections.py`. Смоук-тест 6/6, стабильно (`python -m mining.corrections_smoke_test`): сквозной сценарий (высокий no-show → корректировка `shift_start`; процесс без просрочек → корректировок нет, ложных срабатываний нет) + модульные проверки (`add_checkpoint` при систематическом пропуске, защита от маленькой выборки, влияние порогов). Узкое место (bottleneck) НЕ реализовано в v1 — требует данных о загрузке нескольких исполнителей одной роли, которых нет в закрытой синтетической системе; явно задокументировано как ограничение. Версионирование параметров и применение корректировок — задача C3, здесь только формирование предложений с обоснованием (ФТ-С-7.4, human-in-the-loop)
- [x] C3 (T-42) Агент Аналитик-Адаптер: запуск по завершении цикла (внешний триггер — детектор границы цикла вне объёма T-42), `propose`/`accept-proposal`/`reject-proposal` по каждой корректировке, применение только принятых + режим `auto` для эксперимента (ФТ-С-7.6), уведомление Планировщика/Контролера — `agents/analyst_adapter_agent.py`. Версии — `applied_corrections` (минимальный эквивалент `process_params`, полная версия в T-25); отчёты анализа сохраняются целиком для воспроизводимости (`analysis_reports`, ФТ-А-А-5). Сквозной смоук 16/16, стабильно на 2 прогонах (`python -m agents.analyst_adapter_smoke_test`): агент-«завкафедрой»-заглушка реально принимает `shift_start` и отклоняет прочее через настоящий FIPA-обмен (не заглушка «всегда согласен») — проверены обе ветки решения, плюс auto-режим и «тихий» сценарий без корректировок
- [x] **E3 (часть T-25) — пробел закрыт**: раньше (проверено grep по коду) `scheduler_agent.py`/`controller_agent.py` не содержали НИ ОДНОГО `receive`-поведения — уведомление Аналитик-Адаптера уходило в никуда. Теперь: новая таблица `process_params_current` (СОСТОЯНИЕ — одна строка на процесс с параметрами, действующими прямо сейчас; отдельно от `applied_corrections`, которая остаётся ИСТОРИЕЙ) — `agents/params_store.py` переиспользует тот же маппинг «корректировка → параметр», что уже провалидирован в offline-эксперименте (`experiment.params.apply_corrections`), не дублирует логику. Планировщик перед КАЖДЫМ запуском экземпляра свежо читает `process_params_current` (pull, не кеш — переживает рестарт) и передаёт как `initial_data`; плюс оба агента (`ParamsListener`) реально принимают FIPA `inform` о новой версии (push, для наблюдаемости и протокольной полноты по ФТ-А-А-4). Контролер принимает уведомление, но **честно не** расширяет `watch_activities` динамически из `add_checkpoint` — `Correction` не хранит reminder/escalation-имена, нужные для этого; задокументировано как следующий шаг, не сделанный здесь. Сквозной смоук `python -m agents.params_flow_smoke_test` 6/6, стабильно: (1) Планировщик реально берёт параметры из БД, а не из дефолта правила; (2) полный конвейер `AnalystAdapterAgent.process_cycle(auto)` → `process_params_current` обновлён → оба агента получили уведомление. Регрессия по трём прежним агентским смоукам (B3/B4, C3, B6) — 29/29, без изменений

**Контур адаптации замкнут end-to-end в ЖИВОЙ многоагентной системе**, не только в offline-эксперименте: исполнение (B1) → журнал (B2) → анализ (C1) → корректировки (C2) → применение и уведомление (C3) → Планировщик реально использует новые параметры (E3). Единственный сознательно неполный кусок — Контролер не добавляет новые контрольные точки динамически (см. E3 выше). 52/52 проверок по всем модулям одним прогоном.

**Фаза D — эксперимент**
- [x] D1 (T-50) Дизайн эксперимента: сценарии (baseline/adaptive), метрики, честность сравнения по seed, критерий успеха, явная методологическая оговорка про упрощение shift_start/review_duration — `experiment/DESIGN.md`. Механизм: параметризованные BPMN-таймеры (`bpmn/demo/experiment_process.bpmn`, Python-выражения от `reminder_days`/`escalation_days`) + `experiment/params.py` (маппинг корректировка → параметр). Технический фундамент — спайк параметризации таймеров через `task_tree.data` (`spikes/param_timer_spike/`, 5/5); попутно найден и исправлен реальный баг оркестратора: `start_instance(initial_data=...)` с B1 писал в `wf.data`, который движок при исполнении вообще не читает (независимое от `task.data` хранилище) — параметр молча игнорировался с самого начала
- [ ] D2 (T-43+T-53) Калибровка и валидация генератора на реальном срезе выпуска-2021 — ждёт данных с кафедры (T-02)
- [x] D3 (T-51) Прогоны: базовый режим vs адаптивный — `experiment/run_experiment.py`. Смоук-тест (уменьшенный масштаб) 8/8, дважды независимо подтверждена сходимость (сначала 3/3 seed лучше в ручном прогоне, затем 2/2 в формальном тесте). Находка по пути: с параметрами исполнителя по умолчанию (`delay_median_days=3`) контур адаптации не давал измеримого эффекта — просрочки были почти целиком «неисправимым» no-show, а не задержкой; расширение окна лечит только задержку. Параметры подобраны так, чтобы заметная доля просрочек была исправима (`experiment/DESIGN.md` §5) — задокументировано как значимая методологическая находка, не просто тюнинг. Второй процесс (workload_planning) в эксперимент пока не включён.
  - ⏸️ **Сознательно отложено на самый конец работы над экспериментом** (не блокирует дальнейшую разработку, механизм уже дважды подтверждён смоук-тестами): «боевой» прогон на полный масштаб дизайна (`python -m experiment.run_experiment --cycles 8 --seeds 5`, ~65-70 мин счёта) для итогового `experiment/results.csv`. Запустить в фоне, когда реально понадобятся финальные цифры для главы 4
- [ ] D4 (T-52) Анализ чувствительности к порогам алгоритма — ⏸️ **отложено на самый конец**: требует несколько полных прогонов (по одному на комбинацию порогов/шагов), несколько часов счёта, не блокирует ничего дальше
- [x] D5 (T-54) Графики и таблицы результатов — `experiment/plots.py` (два PNG-графика + сводные таблицы в `experiment/RESULTS.md`). Прогон на данных уменьшенного масштаба (6 циклов × 4 seed, `experiment/results_reduced.csv`) — **улучшение в 4/4 seed(ах)**, среднее изменение escalated_fraction −0.242 (0.342 → 0.100 к последнему циклу), baseline при этом шумно колеблется без тренда. Бонус: график роста параметров показал, что в этом прогоне сработали оба типа корректировки (`shift_start`: escalation_days 14→25; `review_duration`: reminder_days 7→9.5), не только один. Оформление — по методу skill `dataviz`: категориальная палитра валидирована `validate_palette.js` (ΔE 24.7–33.6 при пороге 8 — с большим запасом), plus разные стили линий (пунктир/сплошная) для устойчивости к чёрно-белой печати в тексте диссертации. Финальный `results.csv` (полный масштаб 8×5) по-прежнему отложен — графики не требуют его, можно перестроить на реальных данных, когда прогон будет запущен

**Фаза E — UI и оформление процесса для человека (вне критического пути для защиты)**

Всё научное ядро (фазы A–D) работает и проверено без единой строчки UI — эта фаза нужна, чтобы систему можно было показать «руками» на кафедре или на защите живьём, а не только прогонами тестов. Не блокирует ни один из P0-пунктов диссертации.

- [x] E1 (T-36) Шаблонизатор: .docx из шаблонов кафедры (docxtpl) — `agents/templater_agent.py` + `templates/render.py` (реестр шаблонов, проверка обязательных полей). Реальных форм кафедры нет (запрошены в T-02, не подтверждены) — синтетический плейсхолдер `templates/vkr_admission_order.docx` (генерируется `templates/build_placeholder_template.py`); замена на реальные формы не потребует менять код агента. Первый агент в прототипе, полностью использующий FIPA Request Interaction Protocol (`request → agree → inform-done/failure`, либо сразу `refuse`) — остальные агенты использовали `propose`/`accept-proposal` (T-42) или простой `inform` (T-33/T-34). Смоук-тест `python -m agents.templater_smoke_test` 9/9, стабильно на 2 прогонах: полный контекст → `agree`+`inform-done` с реальным `.docx` (поля подставлены, тегов `{{ }}` не осталось, событие `document_generated` в `event_log` — ФТ-С-8.2); неполный контекст → сразу `refuse` **без** `agree` с точным списком недостающих полей, файл не создаётся (ФТ-А-Ш-2: никогда не формировать неполный документ); неизвестный шаблон → `refuse`. По пути найден и исправлен свежий баг: класс `Behaviour` в SPADE уже резервирует имя атрибута `self.template` (для `set_template()`/фильтрации сообщений) — присвоение своего значения тому же имени в подклассе молча ломало отправку сообщений (непрозрачная ошибка `'key' and 'value' of metadata MUST be strings` в другом месте кода); переименовано в `template_name`
- [x] E2a (T-37, бэкенд) REST-эндпоинты + Интерфейсный агент — **готово**. `agents/interface_agent.py` — реальный мост человек↔МАС (архитектура §5.2), которого раньше не существовало вообще (роль играли одноразовые агенты-заглушки в тестах): принимает FIPA `propose` от Аналитик-Адаптера и кладёт в новую таблицу `pending_decisions` (`Listen`), затем на решение человека через REST реально отправляет `accept-proposal`/`reject-proposal` обратно (`Dispatch`, по той же схеме «смотри в БД» — как у Контролера). `api/corrections.py` — `GET /corrections/pending` (реальные данные, не заглушка — старый эндпоинт с комментарием «T-42 не реализован» удалён) и `POST /corrections/{id}/decide`, повторное решение уже решённого → 409, не тихая перезапись. `api/tasks.py` — `GET /tasks` (все READY-задачи активных экземпляров — упрощение: без фильтра по роли, BPMN-модели не размечены дорожками) и `POST /tasks/{case_id}/{task_name}/complete`, вызывает `Orchestrator` напрямую (задачи не мостятся через FIPA — на другом конце никто не ждёт ответа, в отличие от предложений корректировок). Задачи НЕ идут через Интерфейсного агента — это осознанное архитектурное решение, а не недосмотр (см. докстринг `agents/interface_agent.py`). Сквозной смоук `python -m agents.interface_flow_smoke_test` 14/14, стабильно на 2 прогонах — самый комплексный тест в проекте: реальный `AnalystAdapterAgent` + реальный `InterfaceAgent` + настоящий `FastAPI TestClient` (не мок) прогоняют предложение корректировки от FIPA `propose` до применения через REST-решение человека, плюс отдельно REST-путь для задач и проверка ролевого доступа (403 для роли без прав, 401 без токена). По пути найден и исправлен баг: `AnalystAdapterAgent._propose_and_collect` не клал `process_key` в метаданные `propose`-сообщения (только `_notify_new_version` для Планировщика/Контролера это делала) — REST не мог отфильтровать предложения по процессу; не всплывало раньше, потому что агенты-заглушки в прежних тестах читали только `kind`/`target`. Регрессия по всем 6 прежним агентским/оркестраторным тестам — 67/67 одним прогоном
- [x] E2b (T-37, фронтенд) React-приложение — **готово**. `web/` — минимальный SPA на React 18 + Vite 5 (без роутера/state-библиотек — три экрана не требуют этого): `Login.jsx` (JWT через `POST /auth/token`), `TaskList.jsx` (READY-задачи + кнопка «Выполнить»), `CorrectionsPanel.jsx` (ожидающие корректировки + «Принять»/«Отклонить»), `App.jsx` связывает состояние авторизации и скрывает панель корректировок для ролей, кроме `dept_head`/`admin` (дублирует, а не заменяет, серверную проверку роли в `api/corrections.py`). Dev-прокси (`vite.config.js`) вместо CORS-мидлвари на бэкенде — сознательный выбор, чтобы не трогать `api/main.py` ради дев-окружения. Сборка (`npm run build`) проходит чисто. Проверено не моками, а реальным сквозным HTTP-прогоном через сам dev-прокси на живых контейнерах (Postgres/Prosody): `POST /auth/token` (dept_head) → `GET /me` → `GET /tasks` → `GET /corrections/pending` → `POST /corrections/{id}/decide` (reject) → повторный `GET /corrections/pending` пуст; отдельно проверён ролевой запрет — `secretary` получает 403 на `/corrections/pending`. Визуально в браузере страницу не проверял (в среде агента нет скриншот-инструмента) — открыл `http://127.0.0.1:5173/` в браузере пользователя для ручной проверки
- [x] E3 (часть T-25) Приёмные поведения для `scheduler_agent.py`/`controller_agent.py` — **готово, см. подробности в разделе «Фаза C» под C3**. Теперь E2 (когда дойдём) сможет реально влиять на поведение живых агентов через тот же путь (`process_params_current`), не только писать в БД

## Ограничения

- Реальных журналов исполнения у вуза нет — эксперимент проводится на **синтетических журналах**, параметры которых калибруются по реальным нормативным документам кафедры и (по мере получения) фактическим датам одного выпуска; это зафиксировано как ограничение исследования.
- Классического машинного обучения в ядре нет — намеренно: интеллектуальная составляющая — методы анализа процессов (discovery/conformance). Предиктивный мониторинг сроков — возможное расширение.
- Персональные данные не используются: только обезличенные роли и синтетические исполнители.

## Автор

Магистерская диссертация, ВолгГТУ, кафедра САПРиПК · 2026–2027
