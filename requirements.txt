# Каркас (T-30)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.4
PyJWT>=2.9
python-multipart>=0.0.9
psycopg[binary]>=3.2
redis>=5.0

# BPMN-движок (спайк T-28, оркестратор T-31)
SpiffWorkflow>=3.1
lxml>=5.0

# Журналирование и анализ (T-32, T-40, T-41)
pandas>=2.2
pm4py>=2.7

# Агенты, FIPA ACL поверх XMPP (T-33, T-34)
spade>=4.1

# Генератор синтетических журналов, управляемое время (T-35)
freezegun>=1.5

# Графики и таблицы результатов эксперимента (T-54)
matplotlib>=3.8

# Шаблонизатор (T-36)
docxtpl>=0.19

# Тесты REST-слоя через FastAPI TestClient (T-37)
httpx>=0.27
